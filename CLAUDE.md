# JLPT Grammar CSV 정제 작업 지침

## 파일 기본 정보

- 파일명: `jlpt_grammar.csv`
- 총 955행, 헤더 없음 → **반드시 `header=None`으로 읽는다**
- 첫 행부터 데이터 (N5-001)
- 배치 단위: **90행씩** 순차 처리

| 인덱스 | 내용 |
|--------|------|
| 0 | 항목번호 (N5-001 형식) |
| 1 | 레벨 (N5~N1) |
| 2 | 문법항목 |
| 3 | 접속방법 |
| 4 | 의미 |
| 5 | 예문 1 (ruby 태그 포함) |
| 6 | 예문 1 해석 |
| 7 | 예문 2 (ruby 태그 포함) |
| 8 | 예문 2 해석 |
| 9 | 설명 |

처리 완료 후 인덱스 10에 `card_type` 컬럼을 추가한다.

---

## 오류 판정 기준

### 판정 A — 확정 오류 (즉시 수정)

아래 두 조건을 **모두** 만족하면 확정 오류다:

1. 문법항목(인덱스 2)이 `～` + 아래 키워드 중 하나를 포함
```python
   CONCEPT_KEYWORDS = ["活用", "指示語", "疑問詞", "基本形"]
```
2. 접속방법(인덱스 3)에 `+ ～` 형식의 접속 구조가 없음

**확정 오류 수정 내용:**

| 항목번호 | 문법항목 | 수정 내용 | card_type |
|---------|---------|----------|-----------|
| N5-005 | ～こそあど指示語 | `～` 제거 | `system_table` |
| N5-006 | ～基本疑問詞 | `～` 제거 | `system_table` |
| N5-034 | ～動詞活用基本形 | `～` 제거 | `paradigm_verb` |
| N5-035 | ～い形容詞活用 | `～` 제거 | `paradigm_iadj` |
| N5-038 | ～な形容詞活用 | `～` 제거 | `paradigm_naadj` |

수정 시 문법항목의 `～`만 제거하고, 나머지 열은 건드리지 않는다.

### 판정 B — 검토 필요 (review.md 기록)

아래 중 하나에 해당하면 수정하지 않고 review.md에 기록한다:

- 판정 A의 조건 1만 만족하고 조건 2는 불확실한 경우
- 문법항목이 비어있거나 `～`만 있는 경우
- 동일 배치 안에서 항목번호가 중복되는 경우
- 예문 열(5~8)이 모두 비어있는 경우
- 그 외 판단하기 어려운 이상 패턴

### 판정 C — 정상 (card_type 부여만)

오류가 없는 일반 행은 card_type을 `grammar`로 부여한다.

---

## card_type 정의

| card_type | 대상 |
|-----------|------|
| `grammar` | 일반 문법 항목 전체 |
| `system_table` | こそあど指示語, 基本疑問詞 등 체계 표 항목 |
| `paradigm_verb` | 動詞活用基本形 |
| `paradigm_iadj` | い形容詞活用 |
| `paradigm_naadj` | な形容詞活用 |

---

## paradigm 항목 설계 (참조용)

이 설계는 현재 작업(CSV 정제)에서 직접 구현하지 않는다.
Anki 변환 단계에서 사용할 기준이므로 참조만 한다.

### paradigm_verb — 세트 A (활용형별, 4종 동사 나란히)

て형/た형 제외. 동사 4종: 5단(読む) / 1단(食べる) / する / くる

| 카드 | 5단 | 1단 | する | くる |
|------|-----|-----|------|------|
| ない형 | 読まない | 食べない | しない | こない |
| ます형 | 読みます | 食べます | します | きます |
| 사전형 | 読む | 食べる | する | くる |
| 의지형 | 読もう | 食べよう | しよう | こよう |

### paradigm_verb — 세트 B (て형・た형, 5단 어미 그룹별 분리)

| 카드 | 어미 | 대표 동사 |
|------|------|---------|
| B-1 | く | 書く (※行く 예외: いって) |
| B-2 | ぐ | 泳ぐ |
| B-3 | す | 話す |
| B-4 | む/ぬ/ぶ | 読む・死ぬ・飛ぶ |
| B-5 | つ/る/う | 待つ・帰る・買う |
| B-6 | 1단 | 食べる |
| B-7 | 불규칙 | する・くる |

### paradigm_iadj

대표어 高い 기준. いい → よい 계열 예외 포함.

### paradigm_naadj

대표어 静か 기준. 보통체/정중체 양쪽 포함.

---

## 출력 파일

| 파일명 | 내용 |
|--------|------|
| `jlpt_grammar_fixed.csv` | 수정 완료된 CSV (원본 덮어쓰기 금지) |
| `review.md` | 판정 B 항목 전체 기록 |

### review.md 형식

```markdown
# JLPT Grammar CSV — 검토 필요 항목

생성일시: YYYY-MM-DD HH:MM
대상 파일: jlpt_grammar.csv

---

## [항목번호] 행 {idx}

- **사유**: (판정 B 해당 이유)
- **문법항목**: {값}
- **접속방법**: {값}
- **의미**: {값}
- **현재 상태**: 수정 없이 유지 / card_type: grammar 부여
- **권장 처리**: (판단 근거 또는 확인 요청 내용)

---
```

---

## 처리 규칙 요약

1. 원본 파일을 절대 덮어쓰지 않는다
2. 배치 단위로 처리하되, 각 배치 완료 후 중간 상태를 누적 저장한다
3. 판정이 불확실하면 수정하지 않고 review.md에 기록한다
4. card_type은 모든 행에 빠짐없이 부여한다
5. ruby 태그 등 기존 데이터 형식을 임의로 변경하지 않는다
