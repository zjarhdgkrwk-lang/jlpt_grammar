# JLPT Grammar CSV 작업 지침

## 파일 기본 정보

- 파일명: `jlpt_grammar.csv`
- 총 955행, 헤더 없음 → **반드시 `header=None`으로 읽는다**
- 첫 행부터 데이터 (N5-001)
- 배치 단위: **90행씩** 순차 처리

| 인덱스 | 내용 |
|--------|------|
| 0 | 항목번호 (N5-001 형식) |
| 1 | 레벨 (N5~N1) |
| 2 | 문법항목 |
| 3 | 접속방법 |
| 4 | 의미 |
| 5 | 예문 1 (ruby 태그 포함) |
| 6 | 예문 1 해석 |
| 7 | 예문 2 (ruby 태그 포함) |
| 8 | 예문 2 해석 |
| 9 | 설명 |

처리 완료 후 인덱스 10에 `card_type` 컬럼을 추가한다.

---

## 1단계 작업 — CSV 정제

### 처리 원칙

모든 행을 배치 단위로 읽으면서 **검토・판단・수정을 한 번에** 수행한다.
판단이 필요한 경우에도 작업을 멈추지 않는다.
대신 판단 근거와 처리 결과를 `review.md`에 기록한다.

### 오류 유형과 처리 방법

#### 유형 1 — 확정 오류: 즉시 수정

아래 두 조건을 **모두** 만족하면 확정 오류다:

1. 문법항목(인덱스 2)이 `～` + 아래 키워드 중 하나를 포함
   ```python
   CONCEPT_KEYWORDS = ["活用", "指示語", "疑問詞", "基本形"]
   ```
2. 접속방법(인덱스 3)에 `+ ～` 형식의 접속 구조가 없음

수정: 문법항목의 `～`만 제거하고, 나머지 열은 건드리지 않는다.

| 항목번호 | 문법항목 | 수정 내용 | card_type |
|---------|---------|----------|-----------|
| N5-005 | ～こそあど指示語 | `～` 제거 | `system_table` |
| N5-006 | ～基本疑問詞 | `～` 제거 | `system_table` |
| N5-034 | ～動詞活用基本形 | `～` 제거 | `paradigm_verb` |
| N5-035 | ～い形容詞活用 | `～` 제거 | `paradigm_iadj` |
| N5-038 | ～な形容詞活用 | `～` 제거 | `paradigm_naadj` |

#### 유형 2 — 판단 필요: 최선의 판단으로 즉시 처리

아래 중 하나에 해당하면 작업을 멈추지 않고 판단해 처리한다:

- CONCEPT_KEYWORDS에 해당하지만 접속방법이 불확실한 경우
  → 의미・예문・설명을 종합해 `grammar` 또는 적합한 card_type 부여
- 문법항목이 비어있는 경우
  → card_type `grammar` 부여, review.md에 기록
- 예문 열(5~8)이 모두 비어있는 경우
  → card_type `grammar` 부여, review.md에 기록
- 항목번호 중복 등 구조적 이상
  → 뒤에 오는 행을 우선, review.md에 기록

모든 경우에서 처리 후 review.md에 판단 근거와 결과를 남긴다.

#### 유형 3 — 정상: card_type만 부여

오류 없는 일반 행은 card_type을 `grammar`로 부여한다.

### review.md 역할과 형식

review.md는 "검토 대기 목록"이 아니라 **판단 근거 로그**다.
유형 1의 확정 수정도 포함해 기록한다.

```markdown
# JLPT Grammar CSV — 처리 로그

생성일시: YYYY-MM-DD HH:MM
대상 파일: jlpt_grammar.csv

---

## [항목번호] 행 {idx}

- **유형**: 확정 오류 / 판단 처리
- **판단 근거**: (왜 이렇게 처리했는지)
- **처리 전**: {문법항목 원래 값}
- **처리 후**: {문법항목 수정 값} / 변경 없음
- **card_type**: {부여된 값}

---
```

### 출력 파일

| 파일명 | 내용 |
|--------|------|
| `jlpt_grammar_fixed.csv` | 수정 완료된 CSV |
| `review.md` | 전체 판단 근거 로그 |

원본 `jlpt_grammar.csv`는 절대 덮어쓰지 않는다.

---

## 2단계 작업 — Anki 변환

1단계 완료 후 별도로 수행한다.
`jlpt_grammar_fixed.csv`의 card_type을 기준으로
Anki import용 파일을 생성한다.

### card_type 정의

| card_type | 대상 |
|-----------|------|
| `grammar` | 일반 문법 항목 |
| `system_table` | 체계 표 항목 (こそあど指示語 등) |
| `paradigm_verb` | 動詞活用基本形 |
| `paradigm_iadj` | い形容詞活用 |
| `paradigm_naadj` | な形容詞活用 |

출력 파일:
- `anki_basic.txt` — `grammar` / `system_table` (Basic 노트)
- `anki_cloze.txt` — `paradigm_*` (Cloze 노트)

Anki import 포맷:
- 탭 구분, 한 줄 = 노트 1개
- Basic: `앞면\t뒷면\t태그`
- Cloze: `텍스트\t태그`
- 태그: 항목번호 + 레벨 (예: `N5 N5-012`)

---

### `grammar` — 일반 문법 카드

```
[앞면]  문법항목 + 접속방법
[뒷면]  의미 / 예문1・해석1 / 예문2・해석2 / 설명
```

---

### `system_table` — 체계 표 단일 카드

こそあど指示語, 基本疑問詞처럼 단어들이 대립 체계를 이루는 항목.
관계를 통째로 보는 것이 핵심이므로 분리하지 않는다.
앞면은 HTML 테이블 빈칸 형식, 뒷면은 완성된 표로 변환한다.

**N5-005 こそあど指示語 예시:**

```
[앞면]
こそあど指示語 — 빈칸을 채우시오

        사물    장소      지정 수식
가까운  ( )     ( )       ( ) + 명사
중간    ( )     ( )       ( ) + 명사
먼      ( )     ( )       ( ) + 명사
불명    ( )     ( )       ( ) + 명사

[뒷면]
        사물    장소      지정 수식
가까운  これ    ここ      この
중간    それ    そこ      その
먼      あれ    あそこ    あの
불명    どれ    どこ      どの

* どれ・どこ・どの는 의문형이기도 함
```

---

### `paradigm_verb` — 動詞活用 Cloze

**세트 A**와 **세트 B**로 완전히 분리한다.

#### 세트 A — 활용형별 카드 (4장)

4종류 동사(5단/1단/する/くる)를 나란히 배치.
**て형・た형은 세트 A에서 제외.**

| 카드 | 5단 | 1단 | する | くる |
|------|-----|-----|------|------|
| ない형 | 読まない | 食べない | しない | こない |
| ます형 | 読みます | 食べます | します | きます |
| 사전형 | 読む | 食べる | する | くる |
| 의지형 | 読もう | 食べよう | しよう | こよう |

Cloze 노트 예시 (ない형):
```
ない형:
5단(読む)   → {{c1::読まない}}
1단(食べる) → {{c1::食べない}}
する        → {{c1::しない}}
くる        → {{c1::こない}}
```
활용형마다 노트 1개, c1 하나 → 카드 1장. 세트 A 합계 **4장**.

#### 세트 B — て형・た형 어미 그룹별 카드 (7노트, 14장)

て형과 た형은 음편 패턴이 1:1 대응이므로 한 노트에 함께 넣는다.
5단 동사는 어미 그룹별로 분리. 1단과 불규칙은 별도 노트.

| 노트 | 어미 | 대표 동사 | て형 | た형 |
|------|------|---------|------|------|
| B-1 | く | 書く | 書いて | 書いた |
| B-2 | ぐ | 泳ぐ | 泳いで | 泳いだ |
| B-3 | す | 話す | 話して | 話した |
| B-4 | む/ぬ/ぶ | 読む・死ぬ・飛ぶ | 読んで・死んで・飛んで | 読んだ・死んだ・飛んだ |
| B-5 | つ/る/う | 待つ・帰る・買う | 待って・帰って・買って | 待った・帰った・買った |
| B-6 | 1단 | 食べる | 食べて | 食べた |
| B-7 | 불규칙 | する・くる | して・きて | した・きた |

> **예외:** 行く는 く어미지만 て형이 いって. B-1 노트 설명란에 명기.
> **참고:** ぬ로 끝나는 동사는 현대어에서 死ぬ 한 개뿐.

Cloze 노트 예시 (B-4):
```
5단 동사 む/ぬ/ぶ 어미 — て형・た형

読む → て형: {{c1::読んで}}  た형: {{c2::読んだ}}
死ぬ → て형: {{c1::死んで}}  た형: {{c2::死んだ}}
飛ぶ → て형: {{c1::飛んで}}  た형: {{c2::飛んだ}}

규칙: む/ぬ/ぶ → ん + で/だ
```
각 노트에서 c1(て형) + c2(た형) = 2장. 세트 B 합계 **14장**.

---

### `paradigm_iadj` — い形容詞活用 Cloze

대표어 高い 기준. いい 예외 포함.

```
い형용사 활용 (高い 기준):

부정형     → {{c1::高くない}}
과거형     → {{c2::高かった}}
부정과거형 → {{c3::高くなかった}}
부사형     → {{c4::高く}}

※ いい → よくない / よかった / よくなかった / よく
```

---

### `paradigm_naadj` — な形容詞活用 Cloze

대표어 静か 기준. 보통체/정중체 양쪽 포함.

```
な형용사 활용 (静か 기준):

명사수식형  → {{c1::静かな}} + 명사
부사형      → {{c2::静かに}}
과거(보통)  → {{c3::静かだった}}
과거(정중)  → {{c4::静かでした}}
부정(보통)  → {{c5::静かじゃない}}
부정(정중)  → {{c6::静かじゃないです}}
```
